//组件的propTypes选项 https://facebook.github.io/react/docs/react-api.html#react.proptypes.arrayof
//用来验证组件实例的属性是否符合要求
import React from 'react';
import {ReactDOM} from 'react-dom';
class MyCom extends React.Component {
    constructor(...arg) {
        super(...arg);
    }

}